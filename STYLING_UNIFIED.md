# Оформление блока "Записи звонков" в едином стиле

## ✅ Внесенные изменения

### 1. HTML структура
**Было:** Отдельный класс `calls-table-section` с собственными стилями
**Стало:** Используется класс `call-center-stats` как у блока статистики

**Изменения:**
- Заголовок: `table-header` → `stats-header`
- Заголовок таблицы: `table-title` → `stats-title`
- Период: `table-period` → `stats-period`
- Фильтры: обернуты в карточку `table-controls-card`
- Таблица: обернута в карточку `table-container-card`
- Пагинация: обернута в карточку `pagination-card`

### 2. CSS стили
Добавлены новые классы в едином стиле Material Design 3:

**`.table-controls-card`**
- Фон: `var(--md-sys-color-surface)`
- Скругление: `var(--md-radius-lg)`
- Тень: `var(--md-elevation-1)`
- Граница: `var(--md-sys-color-surface-variant)`

**`.table-container-card`**
- Те же стили, что и у других карточек
- Внутренний скролл для таблицы

**`.pagination-card`**
- Единый стиль с другими блоками
- Адаптивная верстка для мобильных устройств

**`.table-filters-grid`**
- Grid layout как у формы
- Адаптивные колонки

### 3. JavaScript
Обновлен метод `updatePagination()` для работы с новыми классами.

## 🎨 Единый стиль

Теперь блок "Записи звонков" использует:
- ✅ Те же цвета (Material Design 3)
- ✅ Те же отступы (`var(--md-spacing-*)`)
- ✅ Те же скругления (`var(--md-radius-*)`)
- ✅ Те же тени (`var(--md-elevation-*)`)
- ✅ Те же шрифты и размеры
- ✅ Такие же карточки с границами и тенями

## 📱 Адаптивность

Добавлены медиа-запросы для мобильных устройств:
- Фильтры в одну колонку
- Пагинация вертикально
- Уменьшенные отступы

## ✅ Результат

Блок "Записи звонков" теперь полностью соответствует стилю:
- Блока "Статистика колл-центра"
- Формы запроса
- KPI карточек
- Графиков

Все элементы визуально согласованы и выглядят как единое целое.

