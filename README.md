# Asterisk Queue Analytics

Веб-приложение для анализа статистики очередей Asterisk с отслеживанием перезвонов клиентов и агентов.

## 🚀 Возможности

- **Статистика колл-центра**: Общая статистика по звонкам, процент ответа, SLA
- **Отслеживание перезвонов**: 
  - Перезвонил сам (клиент перезвонил после пропущенного звонка)
  - Перезвонили мы (агент перезвонил клиенту)
  - Не обработан (пропущенные звонки без перезвона)
- **Почасовая статистика**: График распределения звонков по часам
- **Детальная таблица**: Полный лог звонков с пагинацией и поиском
- **Воспроизведение записей**: Встроенный аудио-плеер для прослушивания записей разговоров
- **Real-time мониторинг**: Статус системы и текущее время

## 📋 Требования

- Node.js 14+ 
- MySQL/MariaDB с базой данных Asterisk (asteriskcdrdb)
- Доступ к таблицам: `cdr`, `queuelog`

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/maerty1/asterisk-stats.git
cd asterisk-stats
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения (создайте `.env` файл):
```env
DB_HOST=localhost
DB_USER=asterisk
DB_PASSWORD=your_password
DB_NAME=asteriskcdrdb
PORT=3000
OUTBOUND_MIN_LENGTH=4
```

**Параметры конфигурации:**
- `OUTBOUND_MIN_LENGTH` - минимальная длина номера для исходящих звонков (по умолчанию 4)
  - Номера длиннее этого значения считаются исходящими
  - Номера равные или меньше - входящими

4. Запустите приложение:
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 📊 Использование

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Выберите очередь из списка
3. Укажите диапазон дат
4. Нажмите "Сгенерировать отчет"

## 🗂️ Структура проекта

```
asterisk-stats/
├── app.js                 # Главный файл сервера
├── helpers.js            # Вспомогательные функции
├── package.json          # Зависимости проекта
├── views/                # EJS шаблоны
│   ├── index.ejs        # Главная страница
│   └── error.ejs        # Страница ошибок
├── public/               # Статические файлы
│   ├── css/
│   │   └── style.css    # Стили Material Design 3
│   └── js/
│       ├── chart.js     # Логика графиков и таблиц
│       └── status-monitor.js  # Мониторинг статуса
└── .env                  # Конфигурация (не в git)
```

## 🔍 Отслеживание перезвонов

Система автоматически определяет перезвоны для пропущенных звонков:

- **Перезвонил сам**: Клиент перезвонил в течение 2 часов после пропущенного звонка
- **Перезвонили мы**: Агент перезвонил клиенту в течение 2 часов после пропущенного звонка
- **Не обработан**: Пропущенные звонки без перезвонов

## 🛠️ Технологии

- **Backend**: Node.js, Express.js
- **Frontend**: EJS, Material Design 3, SVG графики
- **База данных**: MySQL/MariaDB
- **Стили**: Material Design 3

## 📝 Лицензия

MIT

## 👤 Автор

maerty1

## 🤝 Вклад

Приветствуются Pull Requests! Для больших изменений сначала откройте Issue для обсуждения.

