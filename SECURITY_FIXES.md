# Исправления безопасности - Руководство по применению

## ✅ Выполненные исправления

### 1. Валидация входных данных ✅
- Добавлена функция `validateReportInput()` для валидации всех пользовательских данных
- Проверка формата дат, диапазонов, имен очередей
- Защита от SQL injection через параметризованные запросы

### 2. CORS настройки ✅
- Заменен `*` на список разрешенных доменов из переменной окружения `ALLOWED_ORIGINS`
- По умолчанию разрешен только `http://localhost:3000`
- Добавлена поддержка credentials

### 3. XSS защита ✅
- Добавлено экранирование HTML в шаблонах EJS
- Использование `replace()` для экранирования специальных символов

### 4. Path Traversal защита ✅
- Добавлена функция `validatePathParams()` для валидации путей
- Проверка на `..`, `/`, `\` в именах файлов
- Проверка соответствия даты в имени файла параметрам пути
- Использование `path.resolve()` для проверки, что путь находится внутри базовой директории

### 5. Логирование в production ✅
- Переписан `logger.js` с использованием `winston`
- Логи записываются в файлы `logs/error.log` и `logs/combined.log`
- В production важные события также выводятся в консоль

### 6. Connection Pooling ✅
- Заменено `mysql.createConnection()` на `mysql.createPool()`
- Настроены параметры пула: `connectionLimit`, `waitForConnections`, `enableKeepAlive`
- Улучшена производительность и стабильность

### 7. Rate Limiting ✅
- Добавлен `express-rate-limit` для маршрута `/report`
- Лимит: 100 запросов за 15 минут с одного IP

### 8. Security Headers ✅
- Настроен `helmet` с кастомной CSP политикой
- Защита от XSS, clickjacking, MIME sniffing и других атак

### 9. Улучшенная валидация файлов ✅
- Расширенная проверка формата имен файлов
- Проверка на path traversal
- Валидация range запросов

### 10. Обработка ошибок ✅
- Добавлены try-catch блоки для файловых операций
- Обработка ошибок потоков чтения файлов
- Проверка существования файлов и что это не директории

## 📋 Настройка переменных окружения

Добавьте в ваш `.env` файл:

```env
# CORS - список разрешенных доменов через запятую
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Уровень логирования (debug, info, warn, error)
LOG_LEVEL=info

# Лимит подключений к БД (по умолчанию 10)
DB_CONNECTION_LIMIT=10

# Путь к записям (уже должен быть)
RECORDINGS_PATH=/var/spool/asterisk/monitor
```

## 🔧 Дополнительные улучшения

### Создание директории для логов

При первом запуске будет автоматически создана директория `logs/`. Убедитесь, что у процесса есть права на запись:

```bash
mkdir -p logs
chmod 755 logs
```

### Проверка работы

1. Проверьте, что логи создаются:
   ```bash
   ls -la logs/
   ```

2. Проверьте, что rate limiting работает - сделайте более 100 запросов за 15 минут

3. Проверьте CORS - попробуйте запрос с другого домена

## ⚠️ Важные замечания

1. **Имя базы данных**: Теперь используется валидация имени БД перед использованием в SQL. Убедитесь, что `CDR_DB_NAME` содержит только буквы, цифры и подчеркивания.

2. **Лимит запросов**: По умолчанию установлен лимит в 10000 записей на запрос. При необходимости можно изменить в функции `getQueueCalls()`.

3. **Логирование**: В production логи пишутся в файлы. Регулярно проверяйте размер логов и настройте ротацию.

4. **CORS**: В production обязательно укажите реальные домены в `ALLOWED_ORIGINS`, не оставляйте `localhost`.

## 🚀 Следующие шаги (опционально)

1. Настроить ротацию логов (logrotate)
2. Добавить мониторинг ошибок (Sentry, etc.)
3. Настроить HTTPS
4. Добавить аутентификацию для административных функций
5. Реализовать серверную пагинацию для больших объемов данных

