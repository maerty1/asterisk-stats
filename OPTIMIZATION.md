# 🚀 Руководство по оптимизации производительности

## Обзор

Этот документ описывает возможности оптимизации проекта Asterisk Queue Analytics для улучшения производительности и скорости обработки запросов.

## 🔍 Выявленные узкие места

### 1. База данных

#### Проблемы:
- **Отсутствие индексов** на часто используемых полях (`calldate`, `linkedid`, `queuename`, `time`)
- **Сложные JOIN операции** без оптимизации
- **Множественные OR условия** в batch-запросах для поиска перезвонов
- **Функции в WHERE** (например, `LENGTH(TRIM(...))`) замедляют запросы

#### Решения:
1. **Создать индексы** (см. `optimize-database-indexes.sql`)
2. **Оптимизировать запросы** - избегать функций в WHERE, использовать префиксные индексы
3. **Партиционирование таблиц** по датам (для очень больших БД)

### 2. Последовательная обработка

#### Проблемы:
- **Последовательная обработка очередей** в `generateDailyReport` (цикл `for`)
- **Последовательная проверка перезвонов** для каждого звонка (старая функция `checkCallbacks`)
- **Отсутствие параллелизма** при обработке данных

#### Решения:
1. **Использовать `Promise.all()`** для параллельной обработки очередей
2. **Batch-запросы** уже реализованы в `checkCallbacksBatch`, но можно оптимизировать
3. **Worker threads** для CPU-интенсивных операций (расчет статистики)

### 3. Кэширование

#### Проблемы:
- **Список очередей** загружается при каждом запросе
- **Нет кэширования** результатов запросов
- **Повторные вычисления** статистики

#### Решения:
1. **Кэшировать список очередей** в памяти с TTL
2. **Redis/Memcached** для кэширования результатов запросов
3. **Кэширование статистики** на уровне приложения

### 4. Connection Pool

#### Проблемы:
- **Малый размер пула** (10 соединений)
- **Отсутствие настройки таймаутов**

#### Решения:
1. **Увеличить размер пула** до 20-50 соединений
2. **Настроить таймауты** для запросов
3. **Мониторинг пула** соединений

## 📋 План оптимизации

### Этап 1: База данных (Критично) ⚡

1. **Создать индексы**:
   ```bash
   mysql -u root -p asteriskcdrdb < optimize-database-indexes.sql
   ```

2. **Проверить производительность**:
   ```sql
   EXPLAIN SELECT ... FROM queuelog WHERE queuename = '8001' AND time BETWEEN ...;
   ```

3. **Оптимизировать запросы**:
   - Избегать функций в WHERE (`LENGTH(TRIM(...))` → предварительная обработка)
   - Использовать префиксные индексы для длинных строк
   - Оптимизировать JOIN операции

### Этап 2: Параллелизация (Высокий приоритет) 🔄

1. **Параллельная обработка очередей**:
   ```javascript
   // Вместо:
   for (const queueRow of queues) { ... }
   
   // Использовать:
   await Promise.all(queues.map(async (queueRow) => { ... }));
   ```

2. **Параллельная обработка перезвонов**:
   - Batch-запросы уже реализованы
   - Оптимизировать сложные OR условия

3. **Worker threads для статистики**:
   - Вынести расчет статистики в отдельный поток

### Этап 3: Кэширование (Средний приоритет) 💾

1. **Кэширование списка очередей**:
   ```javascript
   let availableQueues = [];
   let queuesCacheTime = 0;
   const QUEUES_CACHE_TTL = 3600000; // 1 час
   ```

2. **Кэширование результатов запросов**:
   - Использовать Redis для кэширования
   - TTL: 5-15 минут для статистики

3. **Кэширование на уровне БД**:
   - Query cache MySQL (если доступен)

### Этап 4: Оптимизация кода (Низкий приоритет) 🔧

1. **Оптимизация расчетов**:
   - Избегать множественных проходов по массивам
   - Использовать Map/Set для быстрого поиска

2. **Ленивая загрузка**:
   - Загружать данные по требованию
   - Пагинация для больших результатов

3. **Сжатие ответов**:
   - Уже реализовано через `compression()` middleware

## 🎯 Ожидаемые результаты

### После создания индексов:
- ⚡ **Ускорение запросов в 5-10 раз** для больших таблиц
- 📉 **Снижение нагрузки на CPU** базы данных на 30-50%
- ⏱️ **Уменьшение времени ответа** с 2-5 сек до 0.5-1 сек

### После параллелизации:
- 🔄 **Ускорение обработки очередей** в 3-5 раз (для множественных очередей)
- ⚡ **Снижение времени генерации отчетов** с 30-60 сек до 10-20 сек

### После кэширования:
- 💾 **Снижение нагрузки на БД** на 50-70% для повторных запросов
- ⚡ **Мгновенный ответ** для закэшированных данных

## 📊 Мониторинг производительности

### Метрики для отслеживания:

1. **Время выполнения запросов**:
   ```javascript
   const start = Date.now();
   await pool.execute('...');
   console.log(`Query took ${Date.now() - start}ms`);
   ```

2. **Использование пула соединений**:
   ```javascript
   console.log(`Pool: ${pool.pool._allConnections.length} total, ${pool.pool._freeConnections.length} free`);
   ```

3. **Размер результатов**:
   ```javascript
   console.log(`Fetched ${calls.length} calls`);
   ```

### Инструменты:

- **MySQL Slow Query Log** - для выявления медленных запросов
- **EXPLAIN** - для анализа планов выполнения
- **Node.js Profiler** - для профилирования приложения

## ⚠️ Предупреждения

1. **Индексы**:
   - Занимают дополнительное место на диске
   - Замедляют INSERT/UPDATE операции
   - Создание индексов на больших таблицах может занять время

2. **Параллелизация**:
   - Увеличивает нагрузку на БД
   - Может привести к блокировкам
   - Требует больше памяти

3. **Кэширование**:
   - Может привести к устаревшим данным
   - Требует управления инвалидацией кэша

## 🔧 Рекомендуемый порядок внедрения

1. ✅ **Сначала**: Создать индексы (минимальный риск, максимальный эффект)
2. ✅ **Затем**: Оптимизировать запросы (убрать функции из WHERE)
3. ✅ **Далее**: Параллелизация обработки очередей
4. ✅ **Потом**: Кэширование списка очередей
5. ✅ **В конце**: Полноценное кэширование с Redis (если необходимо)

## 📝 Примечания

- Все оптимизации должны тестироваться на тестовой среде
- Рекомендуется делать бэкапы перед применением изменений
- Мониторить производительность до и после оптимизаций

