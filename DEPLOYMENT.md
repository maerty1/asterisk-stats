# 🚀 Инструкция по развертыванию

## Предварительные требования

- Node.js 14.0 или выше
- MySQL/MariaDB с базой данных Asterisk
- Доступ к таблицам `asteriskcdrdb.cdr` и `asteriskcdrdb.queuelog`
- Права на чтение записей разговоров (если используется функция воспроизведения)

## Быстрая установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/maerty1/asterisk-stats.git
cd asterisk-stats
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Заполните обязательные параметры:
- `DB_HOST` - адрес сервера базы данных
- `DB_USER` - пользователь базы данных
- `DB_PASS` - пароль базы данных
- `DB_NAME` - имя базы данных (обычно `asterisk`)

### 4. Запуск приложения

**Разработка:**
```bash
npm start
```

**С автоперезагрузкой (разработка):**
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

## Развертывание в production

### Использование systemd (рекомендуется)

1. Создайте файл сервиса:

```bash
sudo cp asterisk-stats.service /etc/systemd/system/
```

2. Отредактируйте пути в файле сервиса (если необходимо):

```bash
sudo nano /etc/systemd/system/asterisk-stats.service
```

3. Перезагрузите systemd и запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable asterisk-stats
sudo systemctl start asterisk-stats
```

4. Проверьте статус:

```bash
sudo systemctl status asterisk-stats
```

Подробные инструкции по настройке systemd см. в файле `SYSTEMD_SETUP.md`.

### Использование PM2

```bash
# Установка PM2
npm install -g pm2

# Запуск приложения
pm2 start app.js --name asterisk-stats

# Автозапуск при перезагрузке системы
pm2 startup
pm2 save
```

### Использование Docker (планируется)

Docker контейнер будет добавлен в будущих версиях.

## Настройка обратного прокси (Nginx)

Пример конфигурации Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Для HTTPS добавьте SSL сертификаты (Let's Encrypt):

```bash
sudo certbot --nginx -d your-domain.com
```

## Настройка базы данных

### Создание таблицы для email-отчетов

При первом запуске приложение автоматически создаст таблицу `asteriskcdrdb.email_reports`.

Если необходимо создать вручную:

```sql
CREATE TABLE IF NOT EXISTS asteriskcdrdb.email_reports (
    id INT AUTO_INCREMENT PRIMARY KEY,
    queue_name VARCHAR(255) NOT NULL,
    email_address VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY unique_queue_email (queue_name, email_address)
);
```

### Оптимизация производительности

Для больших объемов данных рекомендуется создать индексы. Скрипты оптимизации находятся в файлах:
- `optimize-database-indexes.sql`
- `optimize-queue-indexes.sql`
- `mariadb-optimization-simple.sql`
- `mariadb-large-data-optimization.sql`

Выполните их на вашей базе данных:

```bash
mysql -u root -p asteriskcdrdb < optimize-database-indexes.sql
```

## Настройка email-отчетов

1. Настройте SMTP параметры в `.env` файле
2. Для Gmail используйте "Пароль приложения" вместо обычного пароля
3. Установите расписание отправки в `EMAIL_CRON_SCHEDULE` (формат cron)

## Устранение неполадок

### Приложение не запускается

1. Проверьте логи:
   ```bash
   sudo journalctl -u asterisk-stats -f
   ```

2. Проверьте подключение к базе данных:
   ```bash
   mysql -u DB_USER -p -h DB_HOST DB_NAME
   ```

3. Проверьте переменные окружения:
   ```bash
   cat .env
   ```

### Медленная работа

1. Проверьте индексы в базе данных
2. Увеличьте `DB_CONNECTION_LIMIT` в `.env`
3. Используйте адаптер `mysql2` (наиболее быстрый)

### Проблемы с записями разговоров

1. Проверьте путь `RECORDINGS_PATH` в `.env`
2. Убедитесь, что у приложения есть права на чтение директории:
   ```bash
   ls -la /var/spool/asterisk/monitor
   ```

## Обновление приложения

```bash
git pull origin main
npm install
sudo systemctl restart asterisk-stats
```

## Резервное копирование

Рекомендуется регулярно делать резервные копии:
- Базы данных MySQL/MariaDB
- Файла `.env` с конфигурацией
- Таблицы настроек (если используете встроенную БД)

## Безопасность

- ✅ Никогда не коммитьте `.env` файл в Git
- ✅ Используйте сильные пароли для базы данных
- ✅ Ограничьте доступ к порту 3000 только необходимым IP
- ✅ Используйте HTTPS в production
- ✅ Регулярно обновляйте зависимости: `npm audit` и `npm update`

## Поддержка

При возникновении проблем создайте Issue на GitHub:
https://github.com/maerty1/asterisk-stats/issues

