# Руководство по внесению вклада

Спасибо за интерес к проекту Asterisk Queue Analytics! Мы приветствуем вклад от сообщества.

## Как внести вклад

### Сообщение об ошибке

Если вы обнаружили баг:

1. Проверьте, не создан ли уже Issue с этой проблемой
2. Создайте новый Issue с описанием:
   - Что произошло
   - Ожидаемое поведение
   - Шаги для воспроизведения
   - Версия Node.js, ОС
   - Логи ошибок (если есть)

### Предложение новой функции

1. Создайте Issue с описанием функции
2. Объясните, почему эта функция будет полезна
3. Опишите, как она должна работать

### Pull Request

1. Fork проекта
2. Создайте ветку для вашей функции/исправления:
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. Внесите изменения
4. Убедитесь, что код соответствует стилю проекта
5. Добавьте тесты (если применимо)
6. Обновите документацию (README.md)
7. Закоммитьте изменения:
   ```bash
   git commit -m 'Add: описание изменений'
   ```
8. Push в ваш fork:
   ```bash
   git push origin feature/your-feature-name
   ```
9. Создайте Pull Request

## Стандарты кода

- Используйте ESLint для проверки кода
- Следуйте существующему стилю кодирования
- Добавляйте комментарии для сложной логики
- Обновляйте документацию при изменении API

## Структура коммитов

Используйте префиксы для коммитов:
- `Add:` - новая функция
- `Fix:` - исправление бага
- `Update:` - обновление существующей функции
- `Refactor:` - рефакторинг кода
- `Docs:` - обновление документации
- `Style:` - изменения стиля (форматирование)
- `Test:` - добавление/изменение тестов

## Вопросы?

Если у вас есть вопросы, создайте Issue с меткой `question`.

Спасибо за ваш вклад! 🎉

