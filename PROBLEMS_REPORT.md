# Отчет о проблемных участках проекта Asterisk Stats

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ БЕЗОПАСНОСТИ

### 1. SQL Injection - Потенциальная уязвимость
**Файл:** `app.js:152, 369-370`
**Проблема:** Прямая интерполяция имени базы данных в SQL запрос
```javascript
FROM ${process.env.CDR_DB_NAME}.queuelog
```
**Риск:** Хотя используется переменная окружения, лучше использовать параметризованные запросы или валидацию имени БД.

### 2. Path Traversal - Уязвимость доступа к файлам
**Файл:** `app.js:310-313`
**Проблема:** Конструкция пути к файлам из пользовательского ввода без достаточной валидации
```javascript
const filePath = path.join(
  process.env.RECORDINGS_PATH || '/var/spool/asterisk/monitor',
  year, month, day, filename
);
```
**Риск:** Возможен доступ к файлам вне целевой директории, если параметры не валидированы должным образом.

### 3. Отсутствие валидации входных данных
**Файл:** `app.js:178-182`
**Проблема:** Пользовательские данные используются без валидации
```javascript
const { queue_name, start_date, end_date } = req.body;
const startTime = `${start_date} 00:00:00`;
const endTime = `${end_date} 23:59:59`;
```
**Риск:** Некорректные данные могут вызвать ошибки или проблемы с безопасностью.

### 4. CORS настроен на разрешение всех источников
**Файл:** `app.js:268, 341, 353`
**Проблема:** 
```javascript
'Access-Control-Allow-Origin': '*'
```
**Риск:** Любой сайт может делать запросы к вашему API, что создает риск CSRF и утечки данных.

### 5. XSS уязвимость в шаблонах
**Файл:** `views/index.ejs:80, 264`
**Проблема:** Данные пользователя выводятся без экранирования
```ejs
<option value="<%= queue %>" <%= queue === selectedQueue ? 'selected' : '' %>><%= queue %></option>
```
**Риск:** Если `queue` содержит вредоносный код, он может быть выполнен в браузере.

---

## 🟠 СЕРЬЕЗНЫЕ ПРОБЛЕМЫ

### 6. Логирование отключено в production
**Файл:** `logger.js:3-25`
**Проблема:** Логгер не работает в production режиме
```javascript
if (process.env.NODE_ENV !== 'production') {
  console.log(...args);
}
```
**Риск:** Невозможно отслеживать ошибки и события в production.

### 7. Неэффективное управление подключениями к БД
**Файл:** `app.js:149, 184`
**Проблема:** Создание нового подключения для каждого запроса
```javascript
const connection = await mysql.createConnection(DB_CONFIG);
// ... использование
await connection.end();
```
**Риск:** Низкая производительность, возможны проблемы при высокой нагрузке.

### 8. Отсутствие rate limiting
**Файл:** `app.js`
**Проблема:** `express-rate-limit` установлен в `package.json`, но не используется
**Риск:** Возможны DDoS атаки и злоупотребление API.

### 9. Отсутствие security headers
**Файл:** `app.js`
**Проблема:** `helmet` установлен в `package.json`, но не используется
**Риск:** Приложение уязвимо к различным атакам (XSS, clickjacking и т.д.).

### 10. Недостаточная валидация имени файла
**Файл:** `app.js:300`
**Проблема:** Валидация только по регулярному выражению, без проверки на path traversal
```javascript
if (!filename.match(/^in-\d+-\d+-\d{8}-\d{6}-\d+\.\d+\.mp3$/)) {
```
**Риск:** Возможен обход валидации с помощью специальных символов.

---

## 🟡 ПРОБЛЕМЫ КАЧЕСТВА КОДА

### 11. Дублирование кода
**Файлы:** `chart.js`, `chart-simple.js`, `modern-chart.js`
**Проблема:** Множественные реализации похожей функциональности
**Риск:** Сложность поддержки, возможные расхождения в поведении.

### 12. Отсутствие обработки ошибок в некоторых местах
**Файл:** `app.js:274-360`
**Проблема:** Нет try-catch для операций с файловой системой
```javascript
const stat = fs.statSync(filePath);
```
**Риск:** Приложение может упасть при ошибках доступа к файлам.

### 13. Жестко заданные пути
**Файл:** `app.js:311`
**Проблема:** 
```javascript
process.env.RECORDINGS_PATH || '/var/spool/asterisk/monitor'
```
**Риск:** Негибкость при развертывании.

### 14. Отсутствие проверки существования .env файла
**Файл:** `app.js:1`
**Проблема:** `dotenv` загружается без проверки существования файла
**Риск:** Неявные ошибки при отсутствии файла.

### 15. Неправильная обработка больших результатов
**Файл:** `app.js:199`
**Проблема:** Ограничение только на клиенте, но не на сервере
```javascript
calls: calls.slice(0, 10000),
```
**Риск:** Проблемы с памятью при больших запросах.

### 16. Отсутствие валидации дат
**Файл:** `app.js:181-182`
**Проблема:** Даты используются без проверки корректности и диапазона
**Риск:** Некорректные запросы к БД, возможные ошибки.

### 17. Отсутствие лимита на размер запроса
**Файл:** `app.js:46`
**Проблема:** `body-parser` используется без ограничения размера
**Риск:** Возможны атаки на исчерпание памяти.

### 18. Небезопасное использование innerHTML
**Файлы:** `chart.js`, `modern-audio-player.js`, `modern-chart.js`
**Проблема:** Использование `innerHTML` с пользовательскими данными
**Риск:** XSS уязвимости.

### 19. Отсутствие проверки типов
**Файл:** `app.js:180`
**Проблема:** Данные из `req.body` используются без проверки типов
**Риск:** Runtime ошибки при неожиданных типах данных.

### 20. Неоптимальная работа с памятью
**Файл:** `app.js:376-428`
**Проблема:** Все данные загружаются в память перед обработкой
**Риск:** Проблемы при больших объемах данных.

---

## 🔵 ПРОБЛЕМЫ ПРОИЗВОДИТЕЛЬНОСТИ

### 21. Отсутствие кэширования
**Файл:** `app.js:147-162`
**Проблема:** Список очередей загружается при каждом запуске, но не кэшируется
**Риск:** Лишние запросы к БД.

### 22. Неэффективные SQL запросы
**Файл:** `app.js:364-374`
**Проблема:** Запрос без индексов и оптимизации
**Риск:** Медленная работа при больших объемах данных.

### 23. Отсутствие пагинации на сервере
**Файл:** `app.js:199`
**Проблема:** Все данные загружаются сразу
**Риск:** Медленная загрузка страницы при больших объемах.

---

## 🟢 МЕЛКИЕ ПРОБЛЕМЫ

### 24. Дублирование функций helpers
**Файлы:** `app.js:80-144`, `helpers.js`
**Проблема:** Функции helpers определены в двух местах
**Риск:** Расхождение в логике.

### 25. Неиспользуемые зависимости
**Файл:** `package.json`
**Проблема:** `helmet`, `express-rate-limit`, `express-session` установлены, но не используются
**Риск:** Увеличение размера node_modules без пользы.

### 26. Отсутствие обработки ошибок в Service Worker
**Файл:** `public/js/sw.js:74-79`
**Проблема:** Ошибки сети обрабатываются минимально
**Риск:** Плохой UX при оффлайн режиме.

### 27. Отсутствие валидации на клиенте
**Файл:** `views/index.ejs:73-104`
**Проблема:** Валидация только через HTML5 атрибуты
**Риск:** Легко обходится, нет кастомных сообщений об ошибках.

### 28. Неоптимальная работа с DOM
**Файл:** `chart.js:771-960`
**Проблема:** Множественные обращения к DOM без кэширования
**Риск:** Медленная работа на слабых устройствах.

### 29. Отсутствие обработки edge cases
**Файл:** `app.js:89-95`
**Проблема:** Функция `calculateWaitTime` может вернуть некорректные значения
**Риск:** Неправильные расчеты статистики.

### 30. Несогласованность в именовании
**Файлы:** Разные файлы
**Проблема:** Смешение русского и английского в коде
**Риск:** Сложность поддержки для международной команды.

---

## 📋 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

### Критично (исправить немедленно):
1. Добавить валидацию всех пользовательских входных данных
2. Исправить CORS настройки
3. Добавить экранирование в шаблонах
4. Исправить path traversal уязвимость
5. Включить логирование в production

### Важно (исправить в ближайшее время):
6. Настроить connection pooling для БД
7. Добавить rate limiting
8. Настроить security headers (helmet)
9. Улучшить валидацию имен файлов
10. Добавить обработку ошибок для файловых операций

### Желательно (улучшить при возможности):
11. Рефакторинг дублирующегося кода
12. Добавить серверную пагинацию
13. Оптимизировать SQL запросы
14. Добавить кэширование
15. Улучшить обработку больших объемов данных

---

## 🔧 БЫСТРЫЕ ИСПРАВЛЕНИЯ

### 1. Валидация входных данных
```javascript
// Добавить в app.js после строки 180
const { queue_name, start_date, end_date } = req.body;

// Валидация
if (!queue_name || typeof queue_name !== 'string') {
  return res.status(400).json({ error: 'Invalid queue name' });
}

if (!/^\d{4}-\d{2}-\d{2}$/.test(start_date) || !/^\d{4}-\d{2}-\d{2}$/.test(end_date)) {
  return res.status(400).json({ error: 'Invalid date format' });
}

const startDate = new Date(start_date);
const endDate = new Date(end_date);
if (isNaN(startDate.getTime()) || isNaN(endDate.getTime())) {
  return res.status(400).json({ error: 'Invalid date' });
}
```

### 2. Исправление CORS
```javascript
// Заменить '*' на конкретный домен
'Access-Control-Allow-Origin': process.env.ALLOWED_ORIGIN || 'http://localhost:3000'
```

### 3. Включение Helmet
```javascript
const helmet = require('helmet');
app.use(helmet());
```

### 4. Включение Rate Limiting
```javascript
const rateLimit = require('express-rate-limit');
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // лимит запросов
});
app.use('/report', limiter);
```

### 5. Connection Pooling
```javascript
const pool = mysql.createPool(DB_CONFIG);
// Использовать pool.execute() вместо createConnection()
```

---

*Отчет создан: $(date)*
*Проанализировано файлов: 15+*
*Найдено проблем: 30*

